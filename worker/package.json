{
  "name": "cf-chat-worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --local",
    "deploy": "wrangler deploy",
    "migrate": "wrangler d1 migrations apply cf_chat_db --remote",
    "migrate:local": "wrangler d1 migrations apply cf_chat_db --local"
  },
  "devDependencies": {
    "esbuild": "^0.21.5",
    "typescript": "^5.5.4",
    "wrangler": "^4.31.0"
  },
  "dependencies": {
    "@cloudflare/ai": "^1.1.0",
    "bcryptjs": "^2.4.3",
    "hono": "^4.5.7",
    "itty-router": "^4.0.23",
    "nanoid": "^5.0.7",
    "zod": "^3.23.8"
  }
}
